import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Vercel deployment için optimizasyonlar
  // Next.js 16.1.1 otomatik olarak optimize edilir, ekstra ayar gerekmez
};

export default nextConfig;
